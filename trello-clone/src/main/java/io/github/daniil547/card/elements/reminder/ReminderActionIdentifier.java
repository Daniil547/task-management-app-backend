package io.github.daniil547.card.elements.reminder;

/**
 * <p>Reminder execution actions that can actually be chosen by the user.</p>
 * <p>Things like sending emails, web app notification pop-ups, Android app notifications etc</p>
 */
public enum ReminderActionIdentifier {
    SEND_EMAIL;
}
